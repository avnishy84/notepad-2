# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - master   # change to "main" if that's your default branch

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout repo
      - uses: actions/checkout@v4

      # Setup Node.js (ensure consistent version)
      - uses: actions/setup-node@v4
        with:
          node-version: 20   # use your local dev version if different

      # Install dependencies (requires package-lock.json)
      - run: npm ci

      # Run build
      - run: npm run build

      # Deploy to Firebase Hosting (production/live channel)
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NOTEPAD_2_E34B1 }}
          channelId: live
          projectId: notepad-2-e34b1
